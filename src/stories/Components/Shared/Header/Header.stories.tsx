import { Header } from "@/components/shared/Header";

const meta = {
  title: "Components/Shared/Header",
  component: Header,
  tags: ["autodocs"],
  argTypes: {},
};

export default meta;

type Story = {
  args: {};
};

export const Default: Story = {
  args: {},
};
